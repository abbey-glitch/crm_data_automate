** to power the app
* create a virtual environment: python3 -m venv your virtual name
** install packages using: pip install -r requirements.txt

<!-- SALES DATA ENTRY VIA API ENDPOINT  AND STORE INTO SQL DATABASE THEN SYNCHRONIZE THROUGH CRM TOOL FOR EMAIL AUTOMATIONS AND ANALYSIS -->